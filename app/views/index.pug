html
  head
    title MBTA Commuter Rail Departure Times
    meta( charset="UTF-8")
    meta( name="viewport" content="width=device-width, initial-scale=1")
    link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css")
    link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap-theme.min.css")
    link( rel="stylesheet" href="app/styles/styles.css")
    script(src="https://code.jquery.com/jquery-2.2.4.min.js")
    script(src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js" )
    script(src="vendor/node_modules/core-js/client/shim.min.js")
    script(src="vendor/node_modules/zone.js/dist/zone.js")
    script(src="vendor/node_modules/reflect-metadata/Reflect.js")
    script(src="vendor/node_modules/systemjs/dist/system.src.js")
    script.
      (function(global) {
        // map tells the System loader where to look for things
        var map = {
          'app':                        'app/scripts', // 'dist',
          '@angular':                   'vendor/node_modules/@angular',
          'angular2-in-memory-web-api': 'vendor/node_modules/angular2-in-memory-web-api',
          'rxjs':                       'vendor/node_modules/rxjs'
        };
        // packages tells the System loader how to load when no filename and/or no extension
        var packages = {
          'app':                        { main: 'boot.js',  defaultExtension: 'js' },
          'rxjs':                       { defaultExtension: 'js' },
          'angular2-in-memory-web-api': { main: 'index.js', defaultExtension: 'js' },
        };
        var ngPackageNames = [
          'common',
          'compiler',
          'core',
          'forms',
          'http',
          'platform-browser',
          'platform-browser-dynamic',
          'router',
          'router-deprecated',
          'upgrade',
        ];
        // Individual files (~300 requests):
        function packIndex(pkgName) {
          packages['@angular/'+pkgName] = { main: 'index.js', defaultExtension: 'js' };
        }
        // Bundled (~40 requests):
        function packUmd(pkgName) {
          packages['@angular/'+pkgName] = { main: '/bundles/' + pkgName + '.umd.js', defaultExtension: 'js' };
        }
        // Most environments should use UMD; some (Karma) need the individual index files
        var setPackageConfig = System.packageWithIndex ? packIndex : packUmd;
        // Add package entries for angular packages
        ngPackageNames.forEach(setPackageConfig);
        var config = {
          map: map,
          packages: packages
        };
        System.config(config);
      })(this);

      System.import('app').catch(function(err){ console.error(err); });


  body
    .container
      mbta-app Loading...
        north-station
        south-station